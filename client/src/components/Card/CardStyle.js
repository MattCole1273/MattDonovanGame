import { makeStyles } from '@material-ui/core/styles';

const cardsStyle = makeStyles({
    main: {
        minWidth:'275',
        background:'red',
        marginTop: '12',
        height: '30%'
 
    }
    
})

export default cardsStyle